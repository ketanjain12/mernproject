PORT=5000
DATABASE_URL=postgres://postgres:123456@localhost:5432/projectbackend
JWT_SECRET=change_me_to_a_long_random_secret
JWT_EXPIRES_IN=1d
CORS_ORIGIN=http://localhost:3000

# OTP / Email
# If SMTP is not configured, backend can optionally return OTP in response for dev
OTP_SHOW_IN_RESPONSE=true

SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Optional: allow creating admin via /api/auth/signup only if key matches
# ADMIN_SIGNUP_KEY=some_private_key

SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=Admin@123
SEED_USER_EMAIL=user@example.com
SEED_USER_PASSWORD=User@123
